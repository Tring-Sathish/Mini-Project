table:
  name: jobs
  schema: public
object_relationships:
  - name: jobToCan
    using:
      manual_configuration:
        column_mapping:
          id: jobID
        insertion_order: null
        remote_table:
          name: candidates
          schema: public
  - name: jobToOrg
    using:
      manual_configuration:
        column_mapping:
          org_id: id
        insertion_order: null
        remote_table:
          name: organizations
          schema: public
