CREATE TABLE "public"."jobs" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "jobPosition" text NOT NULL, "officeLocation" text NOT NULL, "department" text NOT NULL, "jobType" text NOT NULL, "numberOfSeats" integer NOT NULL, "salaryRangeFrom" integer NOT NULL, "salaryRangeUpto" integer NOT NULL, "job_description" text NOT NULL, "city" text NOT NULL, "country" text NOT NULL, "org_name" text NOT NULL, "org_id" text NOT NULL, "applicants_no" integer NOT NULL DEFAULT 0, "job_status" text NOT NULL DEFAULT 'Active', "report_status" jsonb NOT NULL, "report_experience" jsonb NOT NULL, "report_educational_level" text NOT NULL, "report_city" text NOT NULL, "report_university" text NOT NULL, "report_male_vs_female" jsonb NOT NULL, PRIMARY KEY ("id") );
CREATE EXTENSION IF NOT EXISTS pgcrypto;
